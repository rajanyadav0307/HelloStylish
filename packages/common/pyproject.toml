[project]
name = "personal-stylist-common"
version = "0.1.0"
description = "Shared schemas and constants for personal-stylist-ai"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
